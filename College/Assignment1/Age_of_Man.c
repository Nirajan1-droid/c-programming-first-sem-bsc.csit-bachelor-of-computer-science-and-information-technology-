#include <stdio.h>
#include <conio.h>
void main()
{
    float age_of_man;                               //identifier can not be seperated by whitespace
    age_of_man = 10.5;
    printf("The age of man is = %.2f", age_of_man);
    getch();//shows the output till key pressed!
    //underscore_ can be used ..but while naming whitespace is not allowed ..these are the identifiers i.e. the variable names.

}
